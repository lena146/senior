#!/bin/bash
cd "$(dirname "$0")"
echo "----------------------------------------"
echo "  시니어 여행 플래너 서버를 켭니다."
echo "  잠시 후 브라우저가 자동으로 열립니다."
echo "  브라우저가 안 열리면 주소창에 입력:"
echo "  http://localhost:8080"
echo "----------------------------------------"
echo "  서버를 끄려면 이 창을 닫거나 Ctrl+C 하세요."
echo "----------------------------------------"
(sleep 2 && open "http://localhost:8080") &
python3 -m http.server 8080
